# URL of your deployed Next.js app (or http://localhost:3000 for local dev)
API_BASE_URL=https://raydealz.onrender.com/
# Must match WORKER_API_KEY in your Next.js .env — used to call worker endpoints
# For the bot we actually only call public + mpesa endpoints so just needs base url

# Optional: restrict which numbers the bot responds to (comma separated, leave empty for all)
ALLOWED_COUNTRY_CODE=254

# Your support phone number shown in messages
SUPPORT_PHONE=+254707253574